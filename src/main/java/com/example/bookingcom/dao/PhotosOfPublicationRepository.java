package com.example.bookingcom.dao;

import com.example.bookingcom.entities.PhotosOfPublication;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PhotosOfPublicationRepository extends JpaRepository<PhotosOfPublication,Long> {
}
